{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the main desktop window; the UI cannot execute arbitrary commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
